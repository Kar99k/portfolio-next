import SectionTemplate from "../templates/SectionTemplate";

const TechStack = () => {
  return (
    <SectionTemplate>
      <div className="" />
    </SectionTemplate>
  );
};

export default TechStack;
